import styled from "styled-components";

export const StyledOrganism = styled.div`
  display: flex;
  flex-direction: column;
  gap: 1em;

  h1 {
    font-size: 1.5rem;
    color: var(--color-primary);
  }
`;
